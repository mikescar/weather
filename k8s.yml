kind: Service
apiVersion: v1
metadata:
  name: ${GCLOUD_PROJECT_NAME}
spec:
  selector:
    app: ${GCLOUD_PROJECT_NAME}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer
---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: ${GCLOUD_PROJECT_NAME}
  labels:
    app: ${GCLOUD_PROJECT_NAME}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ${GCLOUD_PROJECT_NAME}
  template:
    metadata:
      labels:
        app: ${GCLOUD_PROJECT_NAME}
    spec:
      containers:
      - name: ${GCLOUD_PROJECT_NAME}
        image: ${GCLOUD_REGISTRY_DOMAIN}/${GOOGLE_PROJECT_ID}/${GCLOUD_PROJECT_NAME}:${CIRCLE_SHA1}
        ports:
        - name: http
          containerPort: 3000
          protocol: TCP
